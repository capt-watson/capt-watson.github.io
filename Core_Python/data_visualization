#! Representing the data in the form of pictures or graphs
#! is called the 'data visualization'.

#% Bar graph

# import pandas as pd
# import matplotlib.pyplot as plt

# df = pd.read_csv('C:/Users/shekh/Projects/Python_Tutorials/Core_Python/empdata.csv')

#! Display employee id numbers on X-Axis and their salaries on Y-Axis in the form of a bar graph.

# empdata = {'empid':[1001, 1002, 1003, 1004, 1005, 1006],'ename':
#  ['Veronica Cruiz','Ariana White','Grace Smith','Everly John','Valentina Eastwood','Allison Rode'],'sal':[95050.0,65845.0,64215.0,58785.0,26589.5,63485.5],'doj':['14-02-2008','21-05-2012','24-08-2018','21-05-2012','13-09-2021','24-07-2011']}

# ## create a data frame
# df = pd.DataFrame(empdata)

## extract empid and salary data into x and y vars.
# x = df['empid']
# y = df['sal']

# ## Crete a bar graph
# plt.bar(x,y,label='Employee data', color='blue')

# ## set x and y axis labels
# plt.xlabel('Employee Ids')
# plt.ylabel('Employee salaries')

# ## set company title
# plt.title('MICROSOFT Inc')

# ## Show legend
# plt.legend()

# ## display the graph
# plt.show()


#! Display employee id numbers on X-Axis and their salaries on Y-Axis in the form of a bar graph for two department of a company

# import matplotlib.pyplot as plt

## take employee ids and salaries for sales dept

# x = [1001, 1003, 1006, 1007, 1009, 1011]
# y = [10000, 23000.50, 18000.35, 16500.50, 12000.75, 9999.99]

# ## take employee ids and salaries for production department
# x1 = [1002, 1004, 1010, 1008, 1014, 1015]
# y1 = [5000, 6000, 4500.00, 12000, 9000, 10000]

## create bar graphs
# plt.bar(x,y, label='Sales Dept', color='Red')
# plt.bar(x1,y1, label= 'Production Dept.', color='green')

# ## set the labels and legend
# plt.xlabel('Emp Id')
# plt.ylabel('Salaries')
# plt.title('MICROSOFT Inc')
# plt.legend()

# ## Display the bar graph
# plt.show()

#@ Histogram

#% Histogram shows distributions of values. 

#! Display showing the number of employees in a specific age groups

# import matplotlib.pyplot as plt

# ## take individual employees ages and range of ages

# emp_ages =[22, 45, 30, 59, 58, 56, 57, 45, 43, 43, 50, 40, 34, 33, 25, 19]
# bins = [0, 10, 20, 30, 40, 50, 60]

# ## create a histogram of bar type
# plt.hist(emp_ages, bins, histtype='bar', rwidth=0.8, color='cyan')

# #% Here, histtype is in the form of bars.
# #% Other types are: 'barstacked', 'step', 'stepfilled'.

# #% Option 'rwidth' = 0.8 indicates that the bar width is 80%. There will be a gap of 10% space before and after the bar.

# ## set labels
# plt.xlabel('Employee ages')  
# plt.ylabel('no. of employees')
# plt.title('ORACLE CORP')
# plt.legend()

# ## Draw the histogram
# plt.show()

#@ Creating a Pie chart

#! Display a pie chart showing the percentage of employees in each department of a company.

# import matplotlib.pyplot as plt

# ## take the percentage of employees of 4 departments.
# slices = [50, 20, 15, 15]

# depts = ['Sales', 'Production', 'HR', 'Finance']

# cols = ['magenta','cyan','brown','gold']

# ## create a pie chart
# plt.pie(slices, labels=depts, colors=cols, startangle=90, explode=(0,0.2,0,0), shadow=True, autopct='%.1f%%')

# #% labels=depts represents a list of labels
# #% colors=cols represents a list of colors
# #% startangle=90 indicates a pie chart will start at 90° (12 o'clock position). Default is 0° (3 o'clock position).

# #% explode=(0,0.2,0,0] indicates whether the slices in the pie chart should stand out or not. 0 indicates that the corresponding slice will not come out of the chart. Next 0.2 indicates that the second slice should come out slightly. Its values 0.1 to 1 indicate how much a slice should come out of the chart.

# #% Option shadow= True indicates that the pie chart should be displayed with a shadow.

# #% Option autopct= '%.1f%%' indicates how to display the percentages on the slices.

# ## set titles and legend
# plt.title('Wipro')
# plt.legend()

# ## show the pie chart
# plt.show()

#@ Creating a line graph

#! Create a line graph to show the profits of a company in various years.

import matplotlib.pyplot as plt

years = ['2012','2013','2014','2015','2016','2017']
profits = [9, 10, 10.5, 8.8, 10.9, 9.75]

## create a line graph
plt.plot(years, profits, 'blue')

## set titles and labels
plt.title('Mauti Udyog')
plt.xlabel('years')
plt.ylabel('Profits in crores Rs')

## show line chart
plt.show()